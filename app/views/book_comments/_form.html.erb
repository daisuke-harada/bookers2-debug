<%= form_with(model: [book_find, book_comment], method: :post, remote: true) do  |f| %>
  <%= f.text_area :comment, rows:'5', class: "form-control",placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>

